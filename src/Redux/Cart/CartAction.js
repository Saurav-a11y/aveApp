export const addItemCart = (item) => ({
  type: "ADD_ITEM_CART",
  payload: item,
});
export const colorSelected = (color) => ({
  type: "COLOR_SELECTED",
  payload: color,
});
export const sizeSelected = (size) => ({
  type: "SIZE_SELECTED",
  payload: size,
});
